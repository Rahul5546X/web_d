#include <iostream>
using namespace std;
int main(){ 
    int a,b ;
    cout<<"enter the value of divisor and divident respectively " ;
    cin>>a>>b;
    cout<<"the value of divisor is "<< a<<" and the value of divident is "<<b<<" and there remainder is "<< a%b <<" and there quotient is "<<a/b;
    return 0;
}