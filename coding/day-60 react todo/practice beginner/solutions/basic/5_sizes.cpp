#include <iostream>
using namespace std;
int main(){ 
    int a ;
    float b;
    char c;
    double d;

    cout<<"The size of int is "<<sizeof(a);
    cout<<" The size of float is "<<sizeof(b);
    cout<<" The size of char is "<<sizeof(c);
    cout<<" The size of double is "<<sizeof(d);
    
    return 0;
}